#ifndef JOIN_CPP
# define JOIN_CPP

#include "server.hpp"

void join(vector<string> *vec, Server & srv) {
    vector<string>::iterator it = vec->begin();
	if (srv._channel.compare(*it))
	{

	}
}
#endif